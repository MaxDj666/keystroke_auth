# 📋 Краткое резюме для вашего научного труда

## Описание реализованной системы

Данное веб-приложение реализует **систему непрерывной биометрической аутентификации на основе анализа клавиатурного почерка (keystroke dynamics)**.

## Основные компоненты

### 1. Модуль сбора данных (Data Collection)
- Отслеживание keypress и keyrelease событий через JavaScript
- Запись временных меток для каждого события
- Сохранение кодов символов и текста

### 2. Модуль извлечения признаков (Feature Extraction)
```
Дwell Time: время нажатия на клавишу
Flight Time: время между отпусканием и нажатием следующей
Typing Speed: скорость печати (символы/сек)
Pressure Consistency: вариация нажатий
```

### 3. Модуль обучения профиля (Profile Training)
- Сбор минимум 5 образцов клавиатурного почерка
- Вычисление среднего вектора признаков
- Хранение стандартного отклонения для каждого признака

### 4. Модуль верификации (Verification)
- Евклидово расстояние между текущим и профилем
- Преобразование расстояния в оценку подобия (0-1)
- Пороговое значение: 0.6

### 5. Модуль обнаружения аномалий (Anomaly Detection)
- Алгоритм Isolation Forest
- Обнаружение необычных паттернов печати
- Комбинированное решение: (similarity > 0.6) AND (NOT anomaly)

### 6. Модуль непрерывной аутентификации (Continuous Authentication)
- Мониторинг почерка каждые 20 символов
- Автоматическое завершение сессии при обнаружении аномалии
- Система предупреждений (3 попытки перед блокировкой)

## Алгоритмические детали

### Подсчет признаков

```python
def extract_features(keystroke_events: list) -> dict:
    # Dwell times
    dwell_times = [e['release_time'] - e['press_time'] for e in events]
    
    # Flight times
    flight_times = [events[i+1]['press_time'] - events[i]['release_time'] 
                    for i in range(len(events)-1)]
    
    # Typing speed
    total_time = events[-1]['release_time'] - events[0]['press_time']
    typing_speed = len(events) / (total_time / 1000)
    
    # Pressure consistency
    pressure_consistency = np.std(dwell_times)
    
    return {
        'mean_dwell': np.mean(dwell_times),
        'std_dwell': np.std(dwell_times),
        'mean_flight': np.mean(flight_times),
        'std_flight': np.std(flight_times),
        'typing_speed': typing_speed,
        'pressure_consistency': pressure_consistency
    }
```

### Сравнение профилей

```python
def calculate_similarity(vector1, vector2):
    # Нормализация (StandardScaler)
    combined = np.vstack([vector1, vector2])
    scaler = StandardScaler()
    scaled = scaler.fit_transform(combined)
    
    # Евклидово расстояние
    distance = np.linalg.norm(scaled[0] - scaled[1])
    
    # Преобразование в подобие
    similarity = 1 / (1 + distance)
    
    return similarity  # [0, 1]
```

## Метрики оценки

Приложение поддерживает вычисление:

1. **False Acceptance Rate (FAR)**
   - Доля самозванцев, ошибочно принятых
   - FAR = (число неправильно принятых) / (общее число попыток самозванцев)

2. **False Rejection Rate (FRR)**
   - Доля легитимных пользователей, ошибочно отклоненных
   - FRR = (число неправильно отклоненных) / (общее число попыток пользователя)

3. **Equal Error Rate (EER)**
   - Точка, где FAR = FRR
   - Характеризует баланс между безопасностью и удобством

4. **Accuracy**
   - Доля верных классификаций из всех попыток

## Таблица результатов (примерные значения)

| User | Попытки | Правильно | FAR | FRR | Accuracy |
|------|---------|-----------|-----|-----|----------|
| User1 | 10 | 9 | 0.05 | 0.10 | 0.90 |
| User2 | 10 | 8 | 0.10 | 0.20 | 0.80 |
| User3 | 10 | 9 | 0.05 | 0.10 | 0.90 |

## Использованные технологии

- **Backend**: Flask, SQLAlchemy
- **ML/Analytics**: scikit-learn, NumPy
- **Frontend**: HTML5, CSS3, Vanilla JavaScript
- **Database**: SQLite
- **Algorithms**: Isolation Forest, Euclidean Distance, StandardScaler

## Возможные улучшения

1. **Deeper Learning Models**
   - Использование LSTM/CNN для анализа последовательностей
   - Улучшение точности до 95%+

2. **Multi-modal Biometrics**
   - Комбинация с отпечатками пальцев
   - Комбинация с распознаванием лица

3. **Адаптивное обучение**
   - Обновление профиля на основе новых образцов
   - Адаптация к изменениям стиля печати

4. **Cross-platform support**
   - Мобильные приложения (iOS/Android)
   - Различные устройства ввода

5. **Privacy-preserving**
   - Локальное хранение профилей
   - Гомоморфное шифрование

## Публикуемые данные

Система может экспортировать следующие данные:

```csv
username,session_id,timestamp,keycode,key_char,dwell_time,flight_time,typing_speed
user1,session_1,2024-01-01T12:00:00,65,A,75,45,5.2
user1,session_1,2024-01-01T12:00:10,66,B,72,48,5.1
...
```

Эти данные могут использоваться для:
- Публикации в научных журналах
- Обучения собственных моделей ML
- Бенчмаркинга с другими системами
- Валидации исследовательских гипотез

## Заключение

Реализованная система демонстрирует практическое применение keystroke dynamics для:
✓ Аутентификации при входе
✓ Непрерывной аутентификации
✓ Обнаружения несанкционированного доступа
✓ Сбора биометрических данных для научных исследований

Приложение может быть использовано как основа для дипломной работы, научной статьи или коммерческого продукта.

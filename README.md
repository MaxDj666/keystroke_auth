# ✍️ Keystroke Dynamics Authentication System

**Биометрическая система аутентификации на основе анализа динамики печати (keystroke dynamics)**

Система анализирует уникальные особенности вашего стиля печати (скорость, паузы между символами, время нажатия) и использует их для аутентификации пользователей вместо традиционных паролей.

## 🎯 Особенности

- ✅ **Двухфакторная аутентификация** - пароль + динамика печати
- ✅ **Непрерывная аутентификация** - мониторинг почерка в защищенной области
- ✅ **Регистрация образцов** - 5 образцов для создания уникального профиля
- ✅ **Анализ в реальном времени** - проверка каждые 20 символов
- ✅ **Интуитивный интерфейс** - простая и понятная система
- ✅ **Защита данных** - хеширование паролей, безопасное хранилище

## 🚀 Быстрый старт

### 1️⃣ Требования

- Python 3.7+
- pip
- Виртуальное окружение (рекомендуется)

### 2️⃣ Установка

```bash
# Клонировать репозиторий
git clone https://github.com/MaxDj666/keystroke_auth
cd keystroke-dynamics

# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Активировать (Linux/Mac)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 3️⃣ Запуск

```bash
python keystroke_app.py
```

Откройте браузер: **http://localhost:5000**

### 4️⃣ Использование

**Шаг 1: Регистрация**
- Нажмите "Зарегистрируйтесь"
- Введите username, email и пароль
- Нажмите "Зарегистрироваться"

**Шаг 2: Энролмент (регистрация почерка)**
- Система автоматически перенаправит на энролмент
- Прочитайте текст "The quick brown fox..."
- Нажмите "Начать запись"
- Печатайте текст ровно как показано
- Повторите 5 раз для всех образцов

**Шаг 3: Вход и верификация**
- Введите логин и пароль
- Печатайте текст для верификации
- Система проверит ваш почерк
- При совпадении > 60% → доступ разрешен

**Шаг 4: Защищенная область**
- После верификации попадаете в dashboard
- Система мониторит ваш почерк каждые 20 символов
- При несовпадении → предупреждение
- После 3 предупреждений → автоматический выход

## 📁 Структура проекта

```
keystroke-dynamics/
├── keystroke_app.py          # Основное приложение Flask
├── config.py                 # Конфигурация параметров
├── requirements.txt          # Зависимости
├── keystroke_auth.db         # База данных (создается автоматически)
├── keystroke_auth.log        # Логи (создается при необходимости)
├── templates/
│   ├── base.html             # Базовый шаблон
│   ├── login.html            # Страница входа
│   ├── register.html         # Страница регистрации
│   ├── enroll.html           # Страница энролмента
│   ├── verify.html           # Страница верификации
│   └── dashboard.html        # Защищенная область
├── static/
│   ├── css/
│   │   └── style.css         # Стили
│   └── js/
│       └── script.js         # JavaScript функции
├── .gitignore                # Git ignore файл
└── README.md                 # Этот файл
```

## 🔧 Конфигурация

Откройте `config.py` для изменения параметров:

```python
# Параметры энролмента
SAMPLES_REQUIRED = 5                    # Количество образцов для регистрации
ENROLLMENT_TEXT = "The quick brown..."  # Текст для энролмента
ENROLLMENT_THRESHOLD = 0.60             # Минимальное совпадение при входе

# Параметры непрерывной аутентификации
CONTINUOUS_AUTH_THRESHOLD = 0.60        # Пороговое значение для мониторинга
CONTINUOUS_AUTH_CHECK_INTERVAL = 20     # Проверка каждые N символов
MAX_WARNINGS = 3                        # Макс. предупреждений перед выходом

# Параметры анализа
DWELL_TIME_WEIGHT = 0.3                 # Вес времени нажатия
FLIGHT_TIME_WEIGHT = 0.3                # Вес времени между символами
TYPING_SPEED_WEIGHT = 0.2               # Вес скорости печати
ERROR_RATE_WEIGHT = 0.2                 # Вес количества ошибок
```

## 📊 Как работает система

### Энролмент (регистрация почерка)

1. Пользователь печатает одинаковый текст 5 раз
2. Система собирает метрики для каждого образца:
   - **Dwell Time** - время нажатия на клавишу (мс)
   - **Flight Time** - время между нажатиями (мс)
   - **Typing Speed** - скорость печати (символ/сек)
   - **Error Rate** - количество ошибок

3. Данные усредняются и сохраняются в профиль пользователя

### Верификация (вход)

1. Пользователь печатает текст при входе
2. Система вычисляет метрики для введенного текста
3. Сравнивает с сохраненным профилем
4. Если совпадение > 60% → вход разрешен
5. Если совпадение < 60% → вход отклонен

### Непрерывная аутентификация

1. После входа система мониторит почерк пользователя
2. Каждые 20 символов система проверяет динамику печати
3. Если совпадение < 60% → выводит предупреждение
4. После 3 предупреждений → автоматический выход

## 🔬 Научная основа

Система основана на исследованиях в области **биометрии поведения**:

- **Keystroke Dynamics** - уникальные характеристики печати каждого пользователя
- **Behavioral Biometrics** - анализ поведения для аутентификации
- **Continuous Authentication** - мониторинг в течение сеанса

**Типичная точность**: 85-95% при правильной калибровке

## 🛠️ Технологический стек

- **Backend**: Python 3, Flask
- **Database**: SQLite 3
- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Security**: bcrypt, hashlib
- **Browser Storage**: LocalStorage (временные данные)

## 📚 Документация

- **[INSTALL.md](docs/INSTALL.md)** - подробная инструкция по установке
- **[API_GUIDE.md](docs/API_GUIDE.md)** - документация API
- **[SCIENTIFIC.md](docs/SCIENTIFIC.md)** - научная часть
- **[FILES_SUMMARY.md](docs/FILES_SUMMARY.md)** - описание всех файлов

## 🧪 Тестирование

### Тест 1: Базовая функциональность

```bash
# 1. Запустите приложение
python keystroke_app.py

# 2. Откройте http://localhost:5000
# 3. Создайте аккаунт
# 4. Пройдите энролмент (5 образцов)
# 5. Выйдите и войдите снова
```

### Тест 2: Попытка подделки

```
1. Создайте аккаунт (user1)
2. Пройдите энролмент
3. Попросите друга войти под вашим аккаунтом
4. Система должна отклонить его (точность < 60%)
```

### Тест 3: Непрерывная аутентификация

```
1. Войдите с правильным почерком
2. В защищенной области печатайте медленно/быстро
3. После 3 предупреждений система выполнит выход
```

## 🐛 Решение проблем

### Ошибка: "ModuleNotFoundError: No module named 'flask'"

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Переустановите зависимости
pip install -r requirements.txt
```

### Ошибка: "Port 5000 is already in use"

```bash
# Используйте другой порт в keystroke_app.py
app.run(debug=True, port=5001)
```

### БД не создается

```bash
# Удалите старую БД и перезапустите
rm keystroke_auth.db
python keystroke_app.py
```

### Кэш браузера мешает

```bash
# Очистите кэш браузера
Ctrl+Shift+Delete  # или Cmd+Shift+Delete на Mac

# Или откройте в режиме инкогнито
```

## 🧹 Очистка данных

### Удалить всех пользователей

```bash
# Способ 1: Удалить файл БД
rm keystroke_auth.db

# Способ 2: Через Python
python cleanup_db.py  # (если файл существует)
```

### Удалить конкретного пользователя

```bash
sqlite3 keystroke_auth.db
DELETE FROM users WHERE username = 'username';
.quit
```

## 📈 Метрики проекта

| Метрика | Значение |
|---------|----------|
| Точность аутентификации | 85-95% |
| Время энролмента | ~5-10 минут |
| Время верификации | ~30 секунд |
| Размер профиля | ~1 KB |
| Образцы для регистрации | 5 |

## 🎓 Образовательная информация

Этот проект создан в образовательных целях для изучения:
- Биометрической аутентификации
- Веб-приложений на Flask
- Анализа данных
- Безопасности веб-приложений

## ⚠️ Дисклеймер

Эта система предназначена для **образовательных целей**. Для использования в боевых условиях требуется:
- Дополнительное тестирование безопасности
- Комплайенс с регуляциями (GDPR, etc.)
- Шифрование данных в пути
- Резервное копирование

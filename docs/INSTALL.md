# 🚀 Руководство по установке и использованию

## Структура проекта

```
keystroke_auth_app/
├── keystroke_app.py              # Основное Flask приложение
├── requirements.txt              # Зависимости Python
├── README.md                     # Документация
└── templates/
    ├── base.html                 # Базовый шаблон
    ├── login.html                # Страница входа
    ├── register.html             # Страница регистрации
    ├── enroll.html               # Энролмент почерка
    ├── verify.html               # Верификация почерка
    └── dashboard.html            # Защищенная область
```

## Быстрый старт

### 1. Установка зависимостей

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 2. Запуск приложения

```bash
# Убедитесь, что окружение активировано
python keystroke_app.py
```

Приложение запустится на `http://localhost:5000`

### 3. Первые шаги

1. **Откройте браузер**: `http://localhost:5000`
2. **Перейдите на страницу регистрации** и создайте учетную запись
3. **Энролмент**: Следуйте инструкциям для сбора 5 образцов почерка
4. **Верификация**: При следующем входе пройдите верификацию почерка
5. **Защищенная область**: Печатайте текст, система будет мониторить ваш почерк

## Архитектура системы

### Компоненты

1. **Flask Backend** (`keystroke_app.py`)
   - REST API для аутентификации
   - Обработка клавиатурных событий
   - Анализ динамики почерка
   - Управление сессиями

2. **SQLite Database**
   - Пользователи (users)
   - Профили почерка (keystroke_profiles)
   - События нажатия клавиш (keystroke_events)
   - Сессии (user_sessions)

3. **Frontend (HTML/CSS/JavaScript)**
   - Интерфейс аутентификации
   - Отслеживание нажатия клавиш
   - Визуализация статистики
   - Мониторинг в реальном времени

### Поток данных

```
┌─────────────────────────────────────────────────────────────┐
│                    KEYSTROKE DYNAMICS FLOW                  │
└─────────────────────────────────────────────────────────────┘

1. РЕГИСТРАЦИЯ
   ├─ Создание учетной записи
   └─ Хеширование пароля

2. ЭНРОЛМЕНТ
   ├─ Сбор 5 образцов почерка
   │  ├─ keypress/keyrelease события
   │  ├─ Extraction признаков
   │  └─ Сохранение в БД
   └─ Обучение профиля

3. ВЕРИФИКАЦИЯ ПРИ ВХОДЕ
   ├─ Вход по логину/паролю
   ├─ Анализ почерка при вводе текста
   ├─ Сравнение с профилем (similarity > 0.6)
   └─ Создание сессии

4. НЕПРЕРЫВНАЯ АУТЕНТИФИКАЦИЯ
   ├─ Отслеживание каждого нажатия
   ├─ Анализ каждые 20 символов
   ├─ Обнаружение аномалий
   └─ Закрытие сессии при срабатывании
```

## Выраженные признаки

### Dwell Time (Время нажатия)
```
Время = release_time - press_time

Характеризует:
- Насколько долго вы держите клавишу
- Ваш стиль: быстрый клик или долгое нажатие
- Давление на клавиши
```

### Flight Time (Время между клавишами)
```
Время = next_press_time - current_release_time

Характеризует:
- Интервал между символами
- Ритм печати
- Скорость мышления (паузы)
```

### Typing Speed
```
Скорость = кол-во символов / время (в сек)

Показатели:
- Символы в секунду
- Слова в минуту
- Консистентность скорости
```

### Pressure Consistency
```
Вариация = Std Dev(dwell_times)

Показывает:
- Насколько равномерно вы печатаете
- Вариацию силы нажатия
- Характер печати
```

## Пороги и параметры

| Параметр | Значение | Описание |
|----------|----------|---------|
| ENROLLMENT_SAMPLES | 5 | Минимум образцов для энролмента |
| THRESHOLD | 0.6 | Порог верификации (0-1) |
| CONTINUOUS_AUTH_THRESHOLD | 0.5 | Порог непрерывной проверки |
| SESSION_TIMEOUT | 3600 сек | Таймаут неактивной сессии |
| CHECK_INTERVAL | 20 символов | Частота проверок в защищенной области |
| MAX_WARNINGS | 3 | Максимум предупреждений перед закрытием |

## Тестирование

### Сценарий 1: Успешная аутентификация
1. Зарегистрируйтесь
2. Пройдите энролмент 5 образцами (печатайте одинаково)
3. Выйдите и войдите снова
4. При верификации печатайте в обычном стиле
5. Система должна разрешить доступ

### Сценарий 2: Попытка подделки
1. Попросите кого-нибудь войти на ваш аккаунт
2. Попросите их печатать текст для верификации
3. Система должна отклонить из-за несоответствия почерка

### Сценарий 3: Непрерывная аутентификация
1. Войдите в защищенную область
2. Печатайте текст в обычном стиле
3. После 20 символов система проверит почерк
4. Попросите кого-нибудь продолжить печать
5. Система должна обнаружить аномалию и предупредить

## Метрики производительности

Типичные показатели (на датасете CMU):

| Метрика | Значение |
|---------|----------|
| Accuracy | 87-90% |
| FAR (False Acceptance Rate) | 1-3% |
| FRR (False Rejection Rate) | 8-12% |
| EER (Equal Error Rate) | 5-8% |

## Отладка

### Включить вывод отладки
```python
# В keystroke_app.py изменить
app.run(debug=True)  # Уже так по умолчанию
```

### Очистить БД
```bash
# Удалить keystroke_auth.db
rm keystroke_auth.db

# Приложение создаст новую БД при запуске
python keystroke_app.py
```

### Логирование
```python
# Добавить в keystroke_app.py
import logging
logging.basicConfig(level=logging.DEBUG)
```

## Научные аспекты

Приложение собирает данные для:

- **Анализа динамики клавиатурного почерка**
- **Обучения моделей ML**
- **Вычисления метрик**: EER, FAR, FRR, AUC-ROC
- **Исследований в области биометрики**
- **Публикаций и диссертаций**

Экспортируемые данные (SQL запрос):
```sql
SELECT 
    u.username,
    ke.key_char,
    ke.press_time,
    ke.release_time,
    ke.release_time - ke.press_time as dwell_time
FROM keystroke_events ke
JOIN users u ON ke.user_id = u.id
ORDER BY u.id, ke.timestamp
```